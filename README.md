# AQF Backend A2 + A3

Backend for AQF form loading and query execution.

Endpoints:
- GET /health
- GET /forms
- GET /forms/{composition_name}
- GET /query/suggestions/{field_name}
- POST /query/search
